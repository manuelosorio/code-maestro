# This file is generated by Nx.
# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build main-app`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t main-app`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=4100

WORKDIR /app


RUN addgroup --system main-app && \
          adduser --system -G main-app main-app

COPY dist/main-app main-app
RUN chown -R main-app:main-app .


# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
#RUN npm --prefix main-app --omit=dev -f install

CMD [ "node", "main-app/analog/server/index.mjs" ]
